#!/usr/bin/env python3
from setuptools import setup

setup(name = 'relative-urls',
      version = '1.0',
      author = 'Denis Kasak',
      author_email = 'dkasak[at]termina.org.uk',
      url = 'https://github.com/dkasak/relative-urls',
      description = ('A tool to extract web endpoints (relative URLs) from a file.'),
      license = 'ISC',
      scripts = ['relative-urls']
)
